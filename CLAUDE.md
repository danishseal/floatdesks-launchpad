# floatdesks-launchpad

The Float launchpad front end. Next.js 16 app in `app/`, dev on **port 3460**
(3000 is another session's).

## What this is, and what it is not

This tree has three lineages stacked in it and they do not agree:

- `programs/floorlaunch`, `PROTOCOL-MASTER.md`, `ECONOMICS-MASTER.md`, `docs/`:
  the **commas / floorlaunch** Solana collectible launchpad. Historical.
- `app/`: was an **ansem-1 CosmWasm** launchpad fork, being repointed at
  **Float on Robinhood Chain**. This is the live product.
- `LAUNCH-HANDOFF.md`, `app/env.example`: stale. They document
  `NEXT_PUBLIC_FLOORLAUNCH_*` vars that nothing reads.

## Hard rules

- No em dashes anywhere (code, comments, UI copy, commits).
- **One address is hardcoded: the Registry.** Everything else resolves at
  runtime through `app/src/lib/float/registry.ts`. Registry keys are the ASCII
  name right-padded to 32 bytes (`cast format-bytes32-string`), NOT keccak; a
  wrong key returns the zero address instead of reverting, so zero is treated as
  a hard failure. Anything that pins a contract address at build time is a bug:
  ~/float's soak did exactly that and posted prices into a dead oracle for three
  days.
- Nothing here is share-backed. `sharesHeld` is 0 on every market, so copy says
  cash-backed. Never "synthetic" or "derivative"; the nouns are fSHARE, listing,
  market, reserve.
- No invented numbers on the liquidity board. Every column traces to a chain
  call or an indexer row, or it says it does not exist. The page this replaced
  shipped a hardcoded `$18,854,306` TVL and an "EPOCH 42" counter for a vault
  that has no epochs.

## Layout

- `app/src/lib/float/` the chain layer: `networks.ts` (presets, one env var to
  switch), `registry.ts` (resolution + venue detection), `chain.ts` (all reads
  and writes), `abi.ts` (generated from `~/float/contracts/out`).
- `app/src/app/api/float/` server routes: `[...path]` proxies the Float indexer
  and adds block timestamps to trade rows (the indexer stores `block`, no `ts`);
  `pools` assembles the liquidity board.
- `app/src/components/wallet/` two wallet backends filling ONE context
  (`wallet-context.ts`), chosen in `providers.tsx` by `NEXT_PUBLIC_WALLET_MODE`:
  `injected` (MetaMask/Rabby, default) and `privy` (embedded + email, needs
  `NEXT_PUBLIC_PRIVY_APP_ID`). Privy signs through `setWalletClientFactory` in
  `lib/float/chain.ts`, since an embedded wallet is not `window.ethereum`. Do
  not give a backend its own context: `useFloatWallet` is imported from nine
  places and they all read the shared one.

## Backends

Switch with `NEXT_PUBLIC_FLOAT_NETWORK`, or the in-app switcher.

| key | chain | registry | venue |
|---|---|---|---|
| `float-testnet` (default) | 46630 | `0xc300f9B7903FaF66dAC973884965652c61AD05Ae` | TokenLaunchpad |
| `float-mainnet` | 4663 | `0x7134d98596490838FC16e8CA16bC2FDd57aD3202` | CurveFunder (reads only) |

Each network needs its OWN indexer process, since one process indexes one
registry: testnet on **:8462** (`~/float/scripts/soak-up.sh`), mainnet on
**:8463** (same `indexer.js`, `REGISTRY=0x7134d985…`, `DB=mainnet-7134d985.db`).
The origin is part of the network preset, overridable with `FLOAT_INDEXER_ORIGIN`.
The public mainnet RPC throttles `eth_getLogs`, so backfill in small chunks.
Do not pattern-kill node; that machine runs other sessions' validators and keepers.

## Who is working on what

| When (start) | Instance | Working on |
|---|---|---|
| 2026-09-04 | session 017xMDwA | Repointing `app/` off ansem-1 onto Float testnet 46630. Holding: `app/src/lib/float/*`, `app/src/app/api/float/pools` + `[...path]`, `app/src/components/liquidity/*`, `app/src/components/wallet/*`, `components/providers.tsx`, the create wizard, trade panel and token page. Stripped the ansem-only surface (horns, social feed/posts/messages, proposals, vault). |
| 2026-09-04 | session 01LqS83j | DeskHook pools section (DONE, absent until a hook is deployed), then the LP surface: `app/src/lib/float/pools.ts`, `app/src/app/api/float/lp-pools/`, the pools section and per-pool detail page on the liquidity board, and add/remove liquidity through PositionManager (mint + burn verified on a mainnet fork). Enumeration spans BOTH launchers, since three of the four launches live on the superseded CurveFunder 0xD55E56Be that no registry key points at. NOW: **v4 swaps execute.** The graduated-token swap that reverted with empty data was a struct mismatch, not a route or approval problem: the UniversalRouter deployed on 4663 carries one more dynamic field between `path` and `amountIn` than current v4-periphery's ExactInputParams, so our encoding shifted amountIn by a word, the router read zero, took it for OPEN_DELTA and reached for a credit it did not have. Found by tracing its opcodes and reading every calldata word it loaded; `app/scripts/fork-swap.ts` runs both directions against a fork and matches the chain's own quoter to the digit. `v4-router.ts` is mine now, handed over by 017xMDwA; the Trade button is still theirs to wire. |
| 2026-09-05 | session 9979f4b4 (float worktree curve-funder) | **Flipped this app's dev server on :3460 to `float-mainnet`** (was float-testnet) so a mainnet launch can be done from the web, and rebuilt the mainnet indexer on :8463, which had START_BLOCK 54796533 against a first Listings log at 54401768 and so reported zero listings on a chain carrying four. Touching NO app source: this is env + processes only. The one thing I want in `create-token-wizard.tsx` is the `launchNew` path (the wizard only calls `cfTx.launchToken`, so an unlisted company cannot be picked); that file is 017xMDwA's, so I am flagging it rather than editing it. Also patched ~/float/services/indexer/indexer.js with RPC retry/backoff, because a single 429 during backfill killed the process. NOW TAKING `create-token-wizard.tsx` (017xMDwA's, untouched since 07:32 and today's commits are the LP lane) to add the `launchNew` path: the wizard only calls `cfTx.launchToken`, so a company with no listing cannot be picked and the genesis set is unreachable from the web. Diff is additive, plus one new file `app/src/lib/float/catalogue.ts`. Also fixed the ansem leftovers in `top-nav.tsx` (docs + X links). |
| 2026-09-05 | session 014hR8Jr | **Token page.** Three asks, all shipped and verified by render on all five mainnet launches. (1) The AMM/Curve badge next to the title is now the real pair, `SNOOZE / fNTDO2`, from a new `/api/float/token-contracts`; `base_label` could not supply it because this venue hardcodes it to "USDG", so the fSHARE ticker is resolved from the curve's own `underlying` through `Listings.get`. Not uppercased: the leading lowercase f is the part that says fSHARE. (2) **The chart had never had a source on this venue.** `/candles` fell through to the indexer proxy, which does not index `CurveBuy`/`CurveSell` and answered `[]` for every token, so tokens that HAD traded printed "Chart will appear after first trade" over their own trades. New `app/src/app/api/float/candles/route.ts` + `lib/float/curve-trades.ts` build OHLC from `CurveBuy`/`CurveSell` (exact, USDG-denominated) UNION the meme pool's v4 `Swap` logs priced through the token's own USDG/fSHARE pool (a graduated token stops trading on the curve, so curve-only history stops dead at graduation). Verified: SNOOZE's last close equals the live pool price computed independently from `sqrtPriceX96`, and DOZE's likewise. `gen-abi.py` grew an event lane, it had emitted functions + errors and ZERO events, the same shape of gap as the dead error layer (b5ee8fe). (3) Copyable contracts grid under About: token, fSHARE, both pool ids, launcher, PoolManager. **Three silent-fiction bugs found and worked around, all the same root: a fanned-out read with a per-item `.catch()` returns a SHORT list that looks complete.** `lpPools()` answered 6, 8 and 2 pools on three consecutive calls; `cfAllTokensDetailed()` lost the very token the page was open on, which rendered as "Token not found" and as a 404 from my route. Neither logs anything. Fix on my side: never enumerate to answer a question about a KNOWN address. `lib/float/token-owner.ts` probes `curves(token)` on each launcher and returns found/absent/**unreadable** as three distinct answers; `lib/float/token-pools.ts` reads `curve.poolId` + `stockPoolOf` and takes the pool key from the PoolManager's own `Initialize` log. Plus `lib/float/retry.ts` (3 attempts, jittered backoff) on my read paths. `cfLaunchers()` exported from curve-funder.ts (additive). **Left for the owners, not touched:** `lpPools()` and `cfAllTokensDetailed()` still go short under load, so the liquidity board and any token list still under-report intermittently. `fetchCurveFunderTokens` computes `current_price` from the curve formula even for GRADUATED tokens, so the header reads $7.33e-7 for SNOOZE where the pool trades at $2.52e-8, a 29x overstatement that also feeds mcap and the Bullpen table. Also fixed en route: the wallet said "0 CHANSE / <0.0001 Floatdesk" for a USDG balance and a native gas balance (now read from `activeNetwork().quoteSymbol` and the chain's `nativeCurrency`), plus the remaining CHANSE copy in token-explorer and analytics; and `trading-chart-canvas` overwrote every candle's open with the PREVIOUS candle's close, which drew one candle across an hour and four trades and printed "O $269.60" on a candle that opened at $270.24. **LATER, same session.** Removed "My account" from the wallet menu. **Token page grid regrouped:** the rail now SPANS both rows (`xl:row-span-2`) instead of sharing row one. While they shared it the row was the taller of the two, my contracts grid pushed the rail past the chart card (909 vs 674), and the 235px difference showed as a dead band above the Holders panel with the resize handle apparently doing nothing. A `minHeight` on the card fixed the band and broke it the other way (the rail still held the row open, so dragging shorter moved nothing); spanning the rail decouples them. Measured: drag up 200px now gives chart -200 and Holders panel -200, tracking 1:1, dead band 0. Also deleted the empty 700px bottom-right cell, which held proposals before they were stripped. **TOOK `create-token-wizard.tsx`** (9979f4b4's; their work was committed and the tree was clean) for a drag-and-drop logo: new `components/trading/image-drop.tsx`. There is no pinning service here and `uploadImage()` in lib/api.ts is an ansem stub that throws, so the only destination that works is the launch metadata itself, which `buildTokenMetaUri` already writes ON CHAIN as a data: blob. So the file is downscaled to 128px and re-encoded WebP down a quality ladder until it fits 6KB, capped hard at 24KB, because bytes on chain cost `ceil(bytes/32)*20000 + bytes*16` gas. The cost shown comes from the chain's own `getGasPrice()` and says "cost unknown" if that read fails. Verified: a 237KB PNG became a 1.6KB WebP, quoted at 0.00042 ETH against a hand-check of 1,066,224 gas x 0.4026 gwei. The wizard's old hidden `<input type=file>` had no handler at all. **Note on browser verification:** the automation's screenshot space is 1568px wide against a 1920px viewport, so `getBoundingClientRect` coordinates must be scaled by 0.8167 before being passed to a click or drag. Unscaled, clicks land ~22% off and silently hit nothing, which cost me a wrong conclusion that the resize handle was dead. **Two data fixes after that.** (1) `fetchTokenTrades()` was `return []` with no address parameter, so the About panel's windows, buys/sells, volume and buyers/sellers bars, and the Transactions tab were all measurements of an empty array and could not have shown anything else. New `/api/float/token-trades` serves them from the SAME points the chart's candles come from, so the two agree by construction. SCALE TRAP: consumers divide `token_amount` by 1e6, NOT the token's own 1e18, so the token leg is scaled to micro-units on the way out. `fee` is left empty rather than 0, since the events do not break it out and 0 reads as "no fee". Pool swaps take the TRANSACTION's sender, not the Swap event's `sender`, which is the UniversalRouter and would name the same address on every row. (2) New `/api/float/prices`: graduated tokens were priced off the spent curve formula, reading SLEEPY at $2.03K of mcap against a pool trading it at $24.62 (82x) and DOZE at $251.45 against $269.92. **First attempt took the last point of the trade history and blocked the page on a full log scan per token: a 20 second blank screen.** Now it reads `getSlot0` on both pools instead, which is the same number for two calls rather than a history, because `sqrtPriceX96` only moves on a swap. 1.72s cold, 0.01s warm, pool keys cached 10 min. Do NOT assume the token is currency0 in its meme pool: it is on all four launches today purely by address ordering, and the fSHARE is identified as the currency the two pools share. Also **volume moved to its own chart pane**. As an overlay it made the price scale reserve its bottom, and the axis labelled that reserved band by extrapolating BELOW the data, printing -$400 on a price chart; shrinking the margin only moved the threshold. **Final sweep.** (1) **Holders** scanned a fixed 500k-block window because it asked the indexer for the token's first block and this deployment's indexer serves no `/tokens`; the window lands AFTER these tokens' first transfers, the reconcile then failed and every token showed "No holders found". Scans from genesis in one call now, chunking kept as a fallback and skipped on a rate limit. DOZE: reconciles true, 3 holders, PoolManager labelled. (2) **24h volume** was a literal "0"; it now sums the candles' own `v` over the last 24h via `fetchToken24h`, one read shared with the 24h change. DOZE reads $72.01, exactly the About panel's $61.17 + $10.84. Null renders "-", never $0. (3) **Liquidity** showed the CURVE's raise on graduated tokens ($55.04 for DOZE). Now "-" for those. I tried computing the real in-range pool figure and BACKED IT OUT: `activeRange`'s bitmap search took /prices to 41s and, worse, starved the cheap price reads through the shared concurrency gate until they retried out and the route returned `{}`. The liquidity board already owns that computation with its exactness flag. (4) **`lpPools()` fixed**: every read retried, so one refused call no longer drops a whole launch into `unreadable`. Four consecutive calls give 8 pools / 0 unreadable (was 6, 8, 2). Its `meta()` also defaulted `decimals` to 18 on a failed read, which on any USDG pool (6dp) is a silent 1e12 error through every price derived from it; it throws now and the caller turns that into an unreadable row. (5) Deleted dead `TokenSummary`, `tiny`, `changeClass` from the token page. |
| 2026-09-05 | session 9979f4b4 (float worktree curve-funder) | **Pointed this app at mainnet.** `networks.ts`: DEFAULT_NETWORK float-testnet -> float-mainnet, and float-mainnet.indexerOrigin localhost:8463 -> http://195.72.61.234:8463, because Vercel was serving the TESTNET's ten markets while the 36 genesis markets live on 4663. That indexer is now a systemd unit `float-indexer` on ansem val1 (public, read-only, chain data only). Touched `networks.ts` and `create-token-wizard.tsx` + new `catalogue.ts`, all declared. If you want testnet back it is one env var, NEXT_PUBLIC_FLOAT_NETWORK=float-testnet. |
| 2026-09-05 | session 9979f4b4 (chart lane) | **The price chart, both surfaces.** (1) The home preview was never a candlestick chart: `components/utoken/sparkline.tsx` drew a polyline over an area fill, tinted by the coin's 24h direction, so DOZE's one real move rendered as a confident ramp into a long flat. Replaced by `components/trading/candle-preview-chart.tsx`, SVG candles off the same `/candles` series; sparkline.tsx is now a two-line re-export under the old name so home.tsx and scanner.tsx did not have to move, and the props (`up` included) are unchanged. (2) `trading-chart-canvas.tsx` fed `candle.time`, already unix SECONDS, through `new Date(number)`, which reads it as milliseconds: every bucket landed in January 1970 and hourly spacing collapsed to 3.6s, so the time axis printed the same minute under all eight candles. (3) **Carried-forward buckets are no longer drawn as trades.** `toCandles` fills gaps with the previous close, which is right, but DOZE at 1m is 7 trades across 3 of 428 buckets; the other 425 now draw muted instead of green, and both counts are stated above the series. (4) A series whose high equals its low is refused, on both surfaces, and says which of "no trades", "one trade" or "n trades at one price" it is. Chart colours come from the CSS tokens via `getComputedStyle` instead of the neon hex that was on a cream page. Touched only `components/trading/*chart*.tsx` + `components/utoken/sparkline.tsx`; nothing under `lib/`, and the candles route was read and left alone. |
| 2026-09-05 | session 9979f4b4 (ETH buy lane) | **Buy a launched token with native ETH.** New `app/src/lib/float/eth-buy.ts` + `components/trading/floorlaunch-trade-panel.tsx` only. ETH cannot be the input to either venue (`CurveFunder.buy` pulls USDG with `transferFrom`; the v4 buy leg is USDG in), so the route is two transactions, `ETH -> USDG` through `EthSwapper` `0xbD7136C1` and then the curve or the two pools. The half-done state between them is a first-class RESULT, not an error: `runEthBuy` resolves `{kind:"stranded"}` with the swap hash, the exact USDG now in the wallet and why hop two refused, the panel says all of it in place of the buy button, the record survives a reload in localStorage, and one button finishes the buy from that USDG without touching ETH again. Pre-flight refuses to start a half-buy: `tradable()`, `deskBuyRefusal` on the stock leg (`splitBpsOf` sized), `TOTAL_SUPPLY` vs `sold + out`. **Today that pre-flight blocks every ETH buy of FDTEST6584, correctly: the NINTENDO listing is status 1 (SettleOnly), so `CurveFunder.buy` would revert inside the Desk and strand the dollars every time.** Both hops carry a real minimum, and `minTokensOut` is priced at hop one's FLOOR so it is a true across-the-route guarantee. Hop one's pool is DISCOVERED, not assumed: five ETH/USDG pools are live on 4663 (fee 100/200/500/3000/10000) and the best differs by size. **Trap found and fixed while verifying in the browser:** a plain `.catch(() => null)` around each candidate quote made a rate-limited quote indistinguishable from an uninitialised pool, and the first run silently routed 0.05 ETH through the 1% pool at 2,427 USDG/ETH while the 0.01% pool paid 2,478. Reverts are answers, transport faults are retried and then thrown. Also, necessarily: **the panel's plain USDG buy on the curve-funder venue was calling TOKEN_LAUNCHPAD** (`tokenPreviewBuy`/`tx.tokenBuy`) at 18dp, so on mainnet it quoted "-" and could not have bought anything; it goes through `cfPreviewBuy`/`cfTx.buy` at 6dp now. Amounts parse with `parseUnits`, not `Math.round(x*1e18)`, which is inexact above ~0.009 ETH. A null quote no longer falls back to a zero minimum. **NOT touched, still broken:** the SELL leg on this venue is still on TOKEN_LAUNCHPAD, and needs a decision between `sell` (pays fSHARE) and `sellToUsdg` (pays dollars) before it can be wired. Nothing was broadcast: every check here is a read or an eth_call. |
| 2026-09-06 | session 9979f4b4 (privy lane) | **Privy wallet backend, built.** It was documented as a drop-in behind `NEXT_PUBLIC_WALLET_MODE` and referenced by comments in `providers.tsx`, `float-wallet-provider.tsx` and `chain.ts`, but `privy-wallet-provider.tsx` did not exist; the dependency was installed and unused. Now real. New `components/wallet/wallet-context.ts` holds the `FloatWallet` interface, the ONE context and a shared `useWalletBalances`, because two backends with two contexts would have thrown "must be used within FloatWalletProvider" at all nine consumers the moment the mode flipped. `float-wallet-provider.tsx` refactored onto it, behaviour unchanged (verified: :3460 still serves and renders in injected mode). New `privy-wallet-provider.tsx` registers `setWalletClientFactory` so signing goes through Privy's own EIP-1193 provider, **re-requested per signature** because Privy's types state `switchChain` does not update providers already handed out. `embeddedWallets` is nested per chain type in v3 (`{ ethereum: { createOnLogin } }`); the flat v2 shape typechecks as a plain object and silently creates no wallet. Robinhood Chain is declared as `defaultChain`/`supportedChains` or embedded wallets land on Ethereum mainnet. A missing app id renders a context whose `connect()` names the missing env var instead of crashing the site. **NOT YET VERIFIED AGAINST A LIVE PRIVY APP:** no app id exists yet, so the login modal, embedded wallet creation and a signed transaction are all unexercised. Two `next dev` instances cannot share `.next` in Next 16, so the privy-mode smoke test needs the one server restarted with the env var rather than a second port. Touched only `components/wallet/*`, `components/providers.tsx`, `app/env.example`. |
| 2026-09-07 | session f59b76dd (token art lane) | **A launched token's logo, everywhere it is meant to appear.** Two separate failures behind one complaint. (1) **Nothing outside this app has ever been able to see a launch's logo.** It lives only as a `data:` URI inside `TokenMetadata.uriOf`, and no explorer, wallet or terminal calls that, so a token was a bare address everywhere else, before and after graduation. New `app/src/app/api/float/token-image/[address]/` unwraps the data URI and serves the bytes at a plain HTTP URL (502, not 404, on a failed read, so a cache never records "no image"; 302 for an http one), and `app/src/app/api/float/tokenlist/` publishes the Uniswap-standard list those consumers actually ingest, `logoURI` pointing at that route. Reads the chain, not the indexer: the indexer's token rows carry a different launchpad's metadata and are empty here, and a list that silently omits tokens is worse than none. New `(shell)/token/[address]/layout.tsx` does the same job for link unfurls, which is where most people meet a launch: the page is a client component and could not carry metadata, so a shared token link had been unfurling as the bare site title. Falls back to the ERC20's own name and symbol when TokenMetadata is empty (verified: CHECK, launched by `cast` with no metadata, still titles as "Curve Check (CHECK)"). (2) **Every logo was a 128px raster** from `image-drop.tsx`'s `MAX_EDGE`, drawn into cards several hundred px wide, so all of them were upscaled ~3x. 320 now, with `imageSmoothingQuality = "high"` (the default "low" drops source pixels on a big downscale instead of averaging them) and an SVG path through an `<img>` rather than `createImageBitmap`, which bakes a vector to its intrinsic size, sometimes 24px. Rasters are still never upscaled. HARD_MAX_BYTES 24KB and the gas quote are unchanged. **Consolidated the render**, which was sixteen copies of `<img src={token.image}>` sharing three holes: an `ipfs://` URI (which the wizard invites you to paste) that no browser fetches and nothing here resolved, so those launches drew a broken-image glyph forever; no `onError`, so a dead host did the same; and eager decode of forty base64 blobs before first paint. New `components/token/token-art.tsx` (`TokenArt` + `resolveTokenImage`) handles all three and falls back to the symbol's first letter, since in a grid the useful thing is still knowing which token you are looking at. Failure is tracked by URL, not as a boolean, so a recycled row retries. **Held:** `token-art.tsx`, the two API routes, the token layout, `image-drop.tsx`. Touched by one call-site swap each, no logic changed: creator page, token page, token-search, token-card, token-feed, leaderboard, command-search, home, scanner, wallet-assets-ui, create-token-wizard. Verified by render at :3460 on both paths (TEST draws its webp, CHECK draws the "C") and by `curl` on the routes and the OG tags. | **LATER: the open-a-market flow, 2026-09-07.** `contribute()` had a button but the two calls around it did not exist in the app, so a market could be fully funded and still never open: nothing anywhere in `src/` called `pour()`, which is what deposits the raise into the Desk, sizes the OI cap and sets the listing Live. Added `pour`/`pourFunded`/`enqueue`/`owner`/`operators` to `VAULTFUNDER_ABI` (all five confirmed present in the DEPLOYED bytecode first, since the deployed CurveFunder had already proved to be an older build than the source tree), `tx.pour`/`tx.pourFunded`/`tx.enqueue` plus `funderCanEnqueue()` in `chain.ts`, and two sections in `liquidity-detail.tsx`: a permissionless "Ready to open" that appears once funding covers the target, and an owner/operator-gated "Queue a market". `ready` compares the RAW integers, not the display floats, so a market a few micro-units short does not render as ready and then revert. `funderCanEnqueue` checks owner OR operators, matching `onlyOwnerOrOperator`, and returns false on a failed read: a read that will not answer is not permission. **Held:** the funder half of `liquidity-detail.tsx`, `funderCanEnqueue` in chain.ts, `.queueGrid`/`.queueRow`/`.queueControl` in the CSS module. Verified by render: both sections are correctly invisible in the real state (queue empty so target is 0, no wallet so canEnqueue is false), so I forced them on locally to actually look at them, found the first layout wrong (the target input collapsed to "Fun" because `.amountInput` is a 27px display size and two of them shared a row), gave the queue form its own compact controls, confirmed the select carries 39 options = placeholder + 38 non-Live markets with the 2 Live ones excluded, then reverted the override and re-checked that both gates close. Console clean on reload. **NOT done:** nothing calls `enqueue` on chain yet, so the queue is still empty and the contribute button stays inert until someone queues a market. **Then: the liquidity board dropping every market.** It rendered "40 markets could not be read and are not listed above" with "An unknown RPC error occurred." against each. Self-inflicted fan-out, not a chain problem: the route did `Promise.all` over all 40 listings with five reads each, ~200 concurrent requests, and the node rate limited it. Of those five reads only `getListing` had no `.catch()`, so it was the one that took the row down. Three fixes. `getListing` now goes through `readRetrying` (exported alias for `simulateRetrying`, transport-only retry) rather than a bare `readContract`; NOT `withRetry`, which flattens everything into `RpcError` and would erase the `UnknownAsset` revert that distinguishes a genuinely delisted asset from a refused call, the same trap as the earlier chain.ts near-regression. The route uses `mapLimited` (MAX_IN_FLIGHT 4, shared with the retry gate, so a retry cannot burst past it) instead of `Promise.all`. And `revertReason` now names the transport when there is no revert to decode: rate limited / timed out / did not respond, instead of viem's "An unknown RPC error occurred" which read as though the asset were at fault. Verified 40 markets / 0 unreadable on three consecutive uncached calls and by render, no banner. **Held additionally:** `revertReason` and the market fan-out in `pools/route.ts`, `getListing` + `readRetrying` in chain.ts.
